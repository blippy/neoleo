#pragma once

#ifdef __cplusplus
	//#define USE_DECIMAL 0
	#define USE_DECIMAL 1
#else
	#define USE_DECIMAL 1
#endif

#if 0
#if USE_DECIMAL
	#ifdef __cplusplus
		#include <decimal/decimal>
		typedef std::decimal::decimal64::__decfloat64 num;
	#else
		typedef _Decimal64 num;
	#endif

	#define NUM_HUNDREDTH 0.01DD
	#define NUM_TEN 10.0DD
#else
	typedef double num;
	#define NUM_HUNDREDTH 0.01
	#define NUM_TEN 10.0
#endif
#endif

#include <ctype.h>
typedef struct { int64_t v; } num;

