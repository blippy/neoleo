#include <iostream>
#include <map>
#include <string>

using std::cout;
using std::endl;
using std::map;
using std::string;

#include "alt-cells.h"
#include "alt-parse.h"
#include "experimental.h"
void run_experimental_mode()
{
	/*
	set_cell_from_user_text(1, "12+3");
	cout << cell_value_as_string(1) << endl;

	cout << "what is 2*pi\n";
	set_cell_from_user_text(2, "2*pi()");
	cout << cell_value_as_string(2) << endl;

	set_cell_from_user_text(2, "2*cell(1)");
	cout << cell_value_as_string(2) << endl;
	*/
}

